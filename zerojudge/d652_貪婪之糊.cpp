#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#define ll long long int
#define _ ios::sync_with_stdio(false);cin.tie(0);
using namespace std;


int main() {_
    int n;
    cin >> n;
    vector<int>s(n);
    int dp[n][n]
    for (int i = 0; i <n; i++) {
        cin >> s[i];
        dp[i][i] = s[i];
    }
    for (int i = 1; i < n; i ++) {
        for(int j = 0 ; i+j < n; j++)
    }
    return 0;
}
